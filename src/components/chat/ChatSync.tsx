'use client'

/**
 * ChatSync — placeholder. 
 * Cross-device sync via Postgres is removed for now.
 * Chat messages live in localStorage only.
 * TODO: add back sync when DB is ready.
 */
export function ChatSync() {
  return null
}
