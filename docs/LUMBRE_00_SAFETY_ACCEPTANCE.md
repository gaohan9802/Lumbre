# Lumbre 阶段 0 验收报告

> 日期：2026-09-03
> 分支：`codex/lumbre-00-safety-baseline`
> 基线：`62948285f996207cbb33c09ec272e644157dc613`
> 结论：本地代码与自动验证已完成；阶段 0 整体仍等待线上与人工验收。

## 修改了什么

- 完整移除 Galatea MCP 桥接模块、工具定义、执行分支和环境变量说明。
- 生产环境对 `/api/debug/**` 和 `/api/memory/breath-debug` 统一返回 404。
- 只有通过服务端内部密钥验证的请求才能标记为无人值守唤醒；伪造 `_wake`
  返回 403。
- 无人值守唤醒不再获得删除、发邮件或回邮件能力；记忆
  `trace(delete=true)` 也在执行入口被拒绝。
- 建立 Node test + `tsx` 的最小自动测试框架，覆盖鉴权、聊天/流式协议、日记、
  照片、待办、记忆、多端同步、重试去重和唤醒安全。
- 测试和生产构建都强制使用独立临时 `DATA_DIR`，不读写 `/persistent`。
- 增加可验证的完整备份脚本、脱敏脚本、合成脱敏 fixture，以及备份/恢复/回退
  runbook。
- 已将 Zeabur 离线备份下载到 Git 仓库外，记录 SHA-256，并完成 gzip、路径安全、
  完整解包及 JSON 内容校验；实际卷内 1,802 个文件与线上计数一致。
- 已从已验证备份生成 78MB 的生产派生脱敏副本；1,183 个 JSON 类文件通过解析，
  约 145 万个字符串值检查未发现未脱敏残留。618 个 Markdown 和 1 个数据库文件
  因无法可靠结构化脱敏而明确排除。
- 用户已确认旧 Galatea Token 吊销，Zeabur 中的 `GALATEA_TOKEN` 与
  `GALATEA_URL` 已删除。
- 不再跟踪本机绝对路径和缓存状态都会变动的 `tsconfig.tsbuildinfo`。

## 没有修改什么

- 未开始第一块数据层拆分。
- 未更改任何现有 JSON 数据格式或生产数据。
- 未拆分工具、模型管道或前端状态。
- 未更换数据库，未部署生产环境，未操作 Zeabur 卷。
- 未从 Git 历史中重写或删除已泄露的 Token；服务端 Token 已吊销。

## 自动测试结果

- `npm test`：14/14 通过。
- `npm run typecheck`：通过。
- `npm run build`：生产构建通过，构建数据位于一次性临时目录。
- `git diff --check`：通过。
- 备份脚本的 create + SHA-256 verify：通过合成 fixture 测试。
- 脱敏脚本：通过合成 fixture 测试。
- 生产备份：SHA-256
  `884a9ff27d70b57b76a2e7f87e1cc7acce899773b308561479cb579a04678676`；
  gzip 校验、完整解包、危险路径检查和 1,183 个 JSON 类文件解析全部通过。
- `npm run lint`：未通过，因为基线项目没有 ESLint 配置，`next lint` 进入交互式初始化；
  本阶段未自行引入新的 lint 规则。

## 人工验收清单

完整步骤见 `docs/SAFETY_BASELINE_RUNBOOK.md`。以下项目尚待在有权访问的环境完成：

- [x] 对线上 `/persistent` 停写备份，拷贝到卷外并验证可读/可展开。
- [x] 从已验证的线下备份生成生产派生脱敏副本，并进行全量字符串规则检查。
- [x] 在 Galatea 端吊销暴露的 Token；无需创建替代 Token。
- [x] 从 Zeabur 删除旧 `GALATEA_TOKEN` 和 `GALATEA_URL`。
- [ ] 部署预览环境，在手机和电脑完成 runbook 中的全部冒烟测试。
- [ ] 确认生产 debug API 都返回 404。
- [ ] 演练回退到基线 SHA，并进行稳定观察。

## 已知风险

- 旧 Galatea Token 仍在 Git 历史中，但已在服务端吊销。
- 2026-09-03 的 `npm audit --omit=dev` 报告 3 个 high 严重度依赖项：直接依赖
  Next.js 14.2.35，以及 PostCSS/Nanoid 依赖链。npm 给出的自动完整修复是升级到 Next.js 16，属于高风险
  主版本升级，未在阶段 0 擅自执行。
- 备份已做本地完整解包验证，但尚未把它覆盖恢复到独立 Zeabur 测试卷；没有为了
  演练恢复而冒险覆盖生产卷。
- 脱敏副本明确排除了 618 个 Markdown 文件和 1 个数据库文件，不能视为原卷的
  字节级副本；完整恢复只能使用原始离线备份。
- 现有 JSON 并发、路径校验和结构校验风险仍然存在；它们是第一块的工作，本阶段没有越界修复。

## 回退方法

- 代码：重新部署基线 SHA `62948285f996207cbb33c09ec272e644157dc613`，或对本阶段提交做
  `git revert`；不要重写共享历史。
- 数据：本地改动未读写生产数据。若预览/生产验证造成数据变更，先停止写入，再使用
  开工前已验证备份恢复。

## 是否建议进入第一块

暂不建议。备份、脱敏和凭据回收已经完成；仍需提交并部署本阶段分支，在手机和电脑完成
人工冒烟、确认生产 debug API 返回 404，并稳定观察后，再由用户明确验收阶段 0。
