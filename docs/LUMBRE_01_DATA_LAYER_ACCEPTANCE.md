# Lumbre 阶段 1 验收报告

> 日期：2026-09-03
> 分支：`codex/lumbre-01-data-layer`
> 上一阶段基线：`18484a3`
> 结论：数据层施工、自动验证和核心人工验收均已通过；用户明确接受风险并豁免原计划中的 24 小时稳定观察，允许进入合并流程。

## 修改了什么

- 建立 `src/server/data/` 统一数据边界，集中管理数据目录、安全路径、JSON 读取、原子写入、备份、恢复和跨进程文件锁。
- 按领域建立聊天、日记与纸条、照片、记忆、待办、时间线、经期、愿望、券包、推送、自动唤醒、论文和用量等数据仓库。
- API 路由和服务端业务模块不再自行拼接用户提供的文件路径；客户端 ID、日期和文件名统一经过格式校验。
- JSON 写入改为临时文件加原子替换，覆盖或删除前保留旧版本；损坏文件不会拖垮整个领域读取。
- 对共享 JSON 的读改写增加文件锁，防止并发请求互相覆盖；聊天、记忆、推送和小型状态仓库均有跨进程并发测试。
- 保持原有 JSON 文件布局和内容格式兼容，包括聊天会话备份命名和旧单文件聊天数据迁移。
- 所有服务端文件系统访问集中在数据层；`/persistent` 只保留为统一数据目录配置的默认值。

## 没有修改什么

- 未更换数据库，未迁移或改写生产数据格式。
- 未开始工具与权限层、模型网关或前端状态层施工。
- 未接入 Claude Code，未恢复 Galatea。
- 未把生产 `/persistent` 用作测试目录，未修改正式 Zeabur 卷。
- 未在预览环境配置真实模型 API Key；脱敏副本中的渠道名称、地址和凭据保持为 `redacted`。

## 自动验证结果

- `npm test`：68/68 通过。
- `npm run typecheck`：通过。
- `DATA_DIR=/tmp/lumbre-stage1-final-build npm run build`：生产构建通过。
- `git diff --check`：通过。
- 覆盖正常读写、损坏 JSON、缺失或错误结构、路径穿越、危险 ID、非法日期、原子备份、损坏恢复和多进程并发写入。
- 所有测试数据均写入系统临时目录，没有读写生产 `/persistent`。

## 预览环境

- 服务：`lumbre-stage0-preview`
- 分支：`codex/lumbre-01-data-layer`
- 数据：独立 `/persistent` 卷中的生产派生脱敏副本。
- 服务部署、登录、同步、刷新和进程重启均恢复正常 Running。
- 重启后通过页面和已登录业务 API 再次核对测试数据，确认数据保存在挂载卷而非容器临时文件系统。

## 核心人工验收

- [x] 日记：创建 `stage1-preview 日记读写测试`，读取成功；服务重启后终端只读核验返回 `DIARY_OK 1858`。
- [x] 待办：创建 `stage1-preview 待办读写测试`，切换为已完成；服务重启后仍存在且 `done=true`。
- [x] 照片：上传无隐私测试图；服务重启后仍可读取，记录 ID 为 `20260903170340_395f`。
- [x] 记忆：通过部署后的数据层创建 `stage1-preview 记忆读写测试`；即时读取返回 `MEMORY_OK 363249a09ba7`，服务重启后页面及 API 均可读取。
- [x] 聊天同步：创建 `stage1-preview` 测试会话；同步、刷新和服务重启后，用户消息与上游失败提示均完整保留。

## 已知限制与用户风险接受

- 原计划要求预览环境连续稳定运行至少 24 小时。本次未满足该时间门槛；用户在核心人工验收和重启持久化检查全部通过后，明确要求豁免该门槛并继续合并。
- 脱敏数据中的模型渠道已被替换为 `redacted`，因此模型请求返回“Zeabur 暂时无法连接模型上游”。聊天记录仍成功持久化；真实模型回复和模型工具调用未在预览环境验证，且模型网关不属于本阶段改动范围。
- 预览卷保留了上述 `stage1-preview` 测试日记、待办、照片、记忆和聊天会话，便于后续复核；它们不在生产卷中。
- 本轮网页验收使用桌面浏览器，没有单独完成手机端全量回归。

## 回退方法

- 代码：重新部署阶段 0 提交 `18484a3`，或对阶段 1 的合并提交执行 `git revert`；不要重写共享历史。
- 数据：阶段 1 保持原有 JSON 格式，因此代码回退无需迁移数据。若正式部署后出现写入异常，先停止写入，再使用阶段 0 已验证的离线备份恢复。

## 是否建议进入阶段 2

阶段 1 的数据层目标与核心人工验收均已完成。考虑到用户明确豁免 24 小时稳定观察，可以合并阶段 1；正式部署后仍应先观察数据读写与错误日志，再由用户单独授权开始阶段 2。
